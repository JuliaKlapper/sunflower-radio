# --- Python (pi-backend) ---
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# --- Node / Next.js (web) ---
node_modules/
web/out/
web/.next/
web/test-results/
web/playwright-report/
web/playwright/.cache/

# --- OS ---
.DS_Store

# --- Agentic tooling: track config & process evidence, ignore regenerated cache ---
# The .claude/ tooling itself is intentionally tracked as homework evidence of the
# agentic-coding process; only the regenerated TTS audio cache is excluded.
.claude/audio/
